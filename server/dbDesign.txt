DATABASE DESIGN (CURRENT)

Admin Collection:
- _id (ObjectId)
- name (String, required)
- email (String, required, unique)
- password (String, required)
- role (String, default: "admin")

Hospital Collection:
- _id (ObjectId)
- name (String, required)
- email (String, required, unique)
- password (String, required)
- role (String enum: ["hospital"], default: "hospital")
- phone (String, required)
- address (Object)
    street (String)
    city (String)
    state (String)
    zipCode (String)
    country (String)
- website (String, default: "")
- licenseNumber (String, required, unique)
- licenseExpiry (Date)
- description (String, default: "")
- status (String enum: ["pending", "approved", "rejected", "suspended"], default: "pending")
- isVerified (Boolean, default: false)
- registrationDate (Date, default: Date.now)
- beds (Number, default: 0)
- departments (String[])
- createdAt (Date)
- updatedAt (Date)

Patient Collection:
- _id (ObjectId)
- name (String, required)
- email (String, required, unique)
- age (Number, required)
- contactNumber (String, required)
- password (String, required)
- role (String enum: ["patient"], default: "patient")
- hospitalId (ObjectId ref: Hospital, default: null)
- address (String, required)
- createdAt (Date)
- updatedAt (Date)

Doctor Collection:
- _id (ObjectId)
- name (String, required)
- email (String, required, unique)
- age (Number, required)
- contact (String, required)
- contactNumber (String, required)
- password (String, required, select: false)
- role (String enum: ["doctor"], default: "doctor")
- specialization (String, required)
- experience (Number, required)
- timeSlots (Object)
    availableTimeSlots (String[], default: [])
    bookedTimeSlots (String[], default: [])
- address (String, required)
- hospitalId (ObjectId ref: Hospital, required)
- licenseNumber (String, required, unique)
- approvalStatus (String enum: ["pending", "approved", "rejected"], default: "pending")
- isApproved (Boolean, default: false)
- available (Boolean, default: true)
- activeHours (Object)
    start (String HH:mm, default: "09:00")
    end (String HH:mm, default: "17:00")
- unavailableReason (String, default: null)
- createdAt (Date)
- updatedAt (Date)

Appointment Collection:
- _id (ObjectId)
- patientId (ObjectId ref: Patient, required)
- doctorId (ObjectId ref: Doctor, required)
- appointmentDate (Date, required)
- appointmentTime (String, required)
- status (String enum: ["scheduled", "completed", "cancelled"], default: "scheduled")
- medicineDescription (String, default: "")
- diagnosis (String, default: "")
- medicalPrescription (String, default: "")
- paymentMethod (String enum: ["cash", "online", "upi", ""], default: "")
- paymentAmount (Number, default: 0)
- paymentStatus (String enum: ["unpaid", "paid"], default: "unpaid")
- paymentTransactionId (String, default: "")
- paidAt (Date, default: null)
- smsReminderSentAt (Date, default: null)
- whatsappReminderSentAt (Date, default: null)
- lastReminderAttemptAt (Date, default: null)
- reminderLastError (String, default: "")
- visitedAt (Date, default: null)
- createdAt (Date)
- updatedAt (Date)

Payment Collection:
- _id (ObjectId)
- appointmentId (ObjectId ref: Appointment, required, unique, indexed)
- hospitalId (ObjectId ref: Hospital, required, indexed)
- doctorId (ObjectId ref: Doctor, required, indexed)
- patientId (ObjectId ref: Patient, required, indexed)
- collectedBy (ObjectId, required)
- collectedByRole (String enum: ["doctor", "admin"], required)
- paymentMethod (String enum: ["cash", "upi", "online"], required)
- paymentGateway (String enum: ["manual", "razorpay"], default: "manual")
- status (String enum: ["pending", "paid", "failed", "refunded"], default: "pending", indexed)
- currency (String, default: "INR")
- amount (Number, required)
- transactionId (String, default: "")
- razorpayOrderId (String, default: "", indexed)
- razorpayPaymentId (String, default: "")
- razorpaySignature (String, default: "")
- notes (String, default: "")
- paidAt (Date, default: null, indexed)
- createdAt (Date)
- updatedAt (Date)

Ambulance Collection:
- _id (ObjectId)
- vehicleNumber (String, required, unique)
- driverName (String, required)
- driverPhone (String, required)
- hospitalId (ObjectId ref: Hospital, required, indexed)
- location (GeoJSON Point, required)
    type (String enum: ["Point"], default: "Point")
    coordinates (Number[] [lng, lat], required)
- status (String enum: ["AVAILABLE", "BUSY", "OFFLINE"], default: "AVAILABLE", indexed)
- isActive (Boolean, default: true)
- createdAt (Date)
- updatedAt (Date)

EmergencyRequest Collection:
- _id (ObjectId)
- patientId (ObjectId ref: Patient, required, indexed)
- location (GeoJSON Point, required)
    type (String enum: ["Point"], default: "Point")
    coordinates (Number[] [lng, lat], required)
- ambulanceId (ObjectId ref: Ambulance, default: null, indexed)
- hospitalId (ObjectId ref: Hospital, default: null, indexed)
- status (String enum: ["PENDING", "ACCEPTED", "COMPLETED", "CANCELLED"], default: "PENDING", indexed)
- createdAt (Date)
- updatedAt (Date)

AmbulanceTracking Collection (Optional):
- _id (ObjectId)
- ambulanceId (ObjectId ref: Ambulance, required, indexed)
- location (GeoJSON Point, required)
    type (String enum: ["Point"], default: "Point")
    coordinates (Number[] [lng, lat], required)
- timestamp (Date, default: Date.now, indexed)

Important Indexes (Ambulance Module):
- ambulances.location: 2dsphere
- ambulances.status: 1
- ambulances.hospitalId: 1
- emergency_requests.location: 2dsphere
- ambulance_tracking.location: 2dsphere